diff --git a/Cargo.toml b/Cargo.toml
index 0a5481abf..35699f571 100644
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -193,7 +193,7 @@ ndk-sys = "0.5.0"
 #gpu-alloc = { path = "../gpu-alloc/gpu-alloc" }
 
 [patch.crates-io]
-#glow = { path = "../glow" }
+glow = { path = "./glow" }
 #web-sys = { path = "../wasm-bindgen/crates/web-sys" }
 #js-sys = { path = "../wasm-bindgen/crates/js-sys" }
 #wasm-bindgen = { path = "../wasm-bindgen" }
diff --git a/wgpu-hal/Cargo.toml b/wgpu-hal/Cargo.toml
index 35e85f45d..35cc1aa37 100644
--- a/wgpu-hal/Cargo.toml
+++ b/wgpu-hal/Cargo.toml
@@ -128,7 +128,7 @@ log.workspace = true
 
 # backend: Gles
 bytemuck = { workspace = true, optional = true }
-glow = { workspace = true, optional = true }
+glow = { path = "../glow", optional = true }
 
 [dependencies.wgt]
 package = "wgpu-types"
diff --git a/wgpu-hal/src/gles/device.rs b/wgpu-hal/src/gles/device.rs
index 0421adc04..5e7f9e1e8 100644
--- a/wgpu-hal/src/gles/device.rs
+++ b/wgpu-hal/src/gles/device.rs
@@ -107,6 +107,11 @@ impl CompilationContext<'_> {
 }
 
 impl super::Device {
+    /// Get a reference to the glow context.
+    pub fn glow_context(&self) -> &glow::Context {
+        self.shared.context.lock()
+    }
+
     /// # Safety
     ///
     /// - `name` must be created respecting `desc`
@@ -834,7 +839,7 @@ impl crate::Device for super::Device {
                                 0,
                                 format_desc.external,
                                 format_desc.data_type,
-                                None,
+                                glow::PixelUnpackData::Slice(None),
                             );
                             width = max(1, width / 2);
                             height = max(1, height / 2);
@@ -854,7 +859,7 @@ impl crate::Device for super::Device {
                                 0,
                                 format_desc.external,
                                 format_desc.data_type,
-                                None,
+                                glow::PixelUnpackData::Slice(None),
                             );
                             width = max(1, width / 2);
                             height = max(1, height / 2);
@@ -907,7 +912,7 @@ impl crate::Device for super::Device {
                                     0,
                                     format_desc.external,
                                     format_desc.data_type,
-                                    None,
+                                    glow::PixelUnpackData::Slice(None),
                                 );
                             }
                             width = max(1, width / 2);
@@ -926,7 +931,7 @@ impl crate::Device for super::Device {
                                 0,
                                 format_desc.external,
                                 format_desc.data_type,
-                                None,
+                                glow::PixelUnpackData::Slice(None),
                             );
                             width = max(1, width / 2);
                             height = max(1, height / 2);
diff --git a/wgpu-hal/src/gles/mod.rs b/wgpu-hal/src/gles/mod.rs
index 55f853715..9490eb510 100644
--- a/wgpu-hal/src/gles/mod.rs
+++ b/wgpu-hal/src/gles/mod.rs
@@ -118,6 +118,9 @@ use arrayvec::ArrayVec;
 
 use glow::HasContext;
 
+// Re-export glow for external access to glow types (e.g., glow::Texture)
+pub use glow;
+
 use naga::FastHashMap;
 use parking_lot::Mutex;
 use std::sync::atomic::{AtomicU32, AtomicU8, Ordering};
@@ -267,6 +270,35 @@ pub struct TextureFormatDesc {
     pub data_type: u32,
 }
 
+impl TextureFormatDesc {
+    /// Create TextureFormatDesc for common RGBA8 format
+    pub fn rgba8() -> Self {
+        Self {
+            internal: glow::RGBA8,
+            external: glow::RGBA,
+            data_type: glow::UNSIGNED_BYTE,
+        }
+    }
+
+    /// Create TextureFormatDesc for RGBA8 sRGB format
+    pub fn rgba8_srgb() -> Self {
+        Self {
+            internal: glow::SRGB8_ALPHA8,
+            external: glow::RGBA,
+            data_type: glow::UNSIGNED_BYTE,
+        }
+    }
+
+    /// Create TextureFormatDesc for BGRA8 format
+    pub fn bgra8() -> Self {
+        Self {
+            internal: glow::RGBA8,
+            external: glow::BGRA,
+            data_type: glow::UNSIGNED_BYTE,
+        }
+    }
+}
+
 struct AdapterShared {
     context: AdapterContext,
     private_caps: PrivateCapabilities,
@@ -409,6 +441,51 @@ impl Texture {
         }
     }
 
+    /// Get raw glow::Texture handle if this is a texture (not renderbuffer)
+    pub fn raw_handle(&self) -> Option<glow::Texture> {
+        match self.inner {
+            TextureInner::Texture { raw, .. } => Some(raw),
+            _ => None,
+        }
+    }
+
+    /// Create a Texture wrapper from an existing glow::Texture handle.
+    /// SAFETY: The caller must ensure the raw texture is valid and compatible.
+    pub unsafe fn from_raw(
+        raw: glow::Texture,
+        desc: &crate::TextureDescriptor,
+        format_desc: TextureFormatDesc,
+    ) -> Self {
+        let target = Self::get_info_from_desc(desc);
+        Self {
+            inner: TextureInner::Texture { raw, target },
+            drop_guard: None, // External texture, don't drop
+            mip_level_count: desc.mip_level_count,
+            array_layer_count: desc.size.depth_or_array_layers,
+            format: desc.format,
+            format_desc,
+            copy_size: crate::CopyExtent {
+                width: desc.size.width,
+                height: desc.size.height,
+                depth: desc.size.depth_or_array_layers,
+            },
+        }
+    }
+
+    /// Create a Texture wrapper from a raw WebGlTexture.
+    /// This registers the WebGlTexture in the glow context and wraps it.
+    /// SAFETY: The caller must ensure the raw texture is valid and compatible.
+    #[cfg(webgl)]
+    pub unsafe fn from_raw_webgl(
+        gl: &glow::Context,
+        raw_webgl: web_sys::WebGlTexture,
+        desc: &crate::TextureDescriptor,
+        format_desc: TextureFormatDesc,
+    ) -> Self {
+        let raw = gl.texture_from_raw(raw_webgl);
+        Self::from_raw(raw, desc, format_desc)
+    }
+
     /// Returns the `target`, whether the image is 3d and whether the image is a cubemap.
     fn get_info_from_desc(desc: &TextureDescriptor) -> u32 {
         match desc.dimension {
diff --git a/wgpu-hal/src/gles/queue.rs b/wgpu-hal/src/gles/queue.rs
index fc106eb23..f1bd47920 100644
--- a/wgpu-hal/src/gles/queue.rs
+++ b/wgpu-hal/src/gles/queue.rs
@@ -744,7 +744,7 @@ impl super::Queue {
                             buffer_data = src.data.as_ref().unwrap().lock().unwrap();
                             let src_data =
                                 &buffer_data.as_slice()[copy.buffer_layout.offset as usize..];
-                            glow::PixelUnpackData::Slice(src_data)
+                            glow::PixelUnpackData::Slice(Some(src_data))
                         }
                     };
                     if is_layered_target(dst_target) {
@@ -889,7 +889,7 @@ impl super::Queue {
                         None => {
                             buffer_data = dst.data.as_ref().unwrap().lock().unwrap();
                             let dst_data = &mut buffer_data.as_mut_slice()[offset as usize..];
-                            glow::PixelPackData::Slice(dst_data)
+                            glow::PixelPackData::Slice(Some(dst_data))
                         }
                     };
                     unsafe {
@@ -1549,7 +1549,7 @@ impl super::Queue {
                 unsafe {
                     gl.bind_image_texture(
                         slot,
-                        binding.raw,
+                        Some(binding.raw),
                         binding.mip_level as i32,
                         binding.array_layer.is_none(),
                         binding.array_layer.unwrap_or_default() as i32,
diff --git a/wgpu-hal/src/gles/web.rs b/wgpu-hal/src/gles/web.rs
index 06bd87124..d2f42b4df 100644
--- a/wgpu-hal/src/gles/web.rs
+++ b/wgpu-hal/src/gles/web.rs
@@ -22,6 +22,11 @@ impl AdapterContext {
     pub fn lock(&self) -> &glow::Context {
         &self.glow_context
     }
+
+    /// Get raw WebGL2 context for direct WebGL operations
+    pub fn raw_context(&self) -> &web_sys::WebGl2RenderingContext {
+        &self.webgl2_context
+    }
 }
 
 #[derive(Debug)]
